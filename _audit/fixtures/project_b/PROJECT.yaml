---
name: synthetic_project_b_fixture
description: Test-only neutral project manifest used to prove reusable onboarding
version: 1.0
metadata:
  type: test_fixture
  status: synthetic

identity:
  project_id: "project_b"
  display_name: "Synthetic Project B"
  repository: "example/project-b"

context:
  sources:
    required: true
    path: "_audit/fixtures/project_b/sources/SOURCES.yaml"
  presentation:
    required: false
    authority:
      path: "_audit/fixtures/project_b/presentation/PRESENTATION_SYSTEM.md"

ownership:
  rule: >
    Project-specific facts belong under this synthetic project root. Generic
    framework behavior remains global and must not embed facts from this project.

capabilities:
  rule: >
    Missing optional capabilities are valid unless a requested task requires them.

---
status: SYNTHETIC_TEST_FIXTURE
