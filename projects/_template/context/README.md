# Additional project context

Use this domain only for long-lived project context that has no narrower canonical owner.

Do not use it as a dumping ground. Dynamic current state belongs in registered sources; reusable global behavior belongs in global authorities.
