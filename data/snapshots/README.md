# Snapshots

Explicit point-in-time captures of data.

Use a snapshot only when the task requires historical state or when a live source
cannot reproduce the required past state. Record capture time and source.
